class Gradebook

    attr_reader :instructor, :coruses, :name

    def initialize(attrs)
        @instructor = instructor
        @courses = []
        @name = name[]
    end

    def add_courses
        @courses += 1
    end


end
